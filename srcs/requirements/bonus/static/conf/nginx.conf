events {}

http {
    default_type application/octet-stream;

    server {
        listen 8081;

        root /var/www/html;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
